name: Renovate

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@latest
    - name: Run Renovate
      uses: renovatebot/github-action@v40.1.11
      with:
        token: ${{ github.token }}
